import { companyResultSchema, companyReadSchema, companiesPageSchema, companyContactsPageSchema, type CompanyCommand, type CompanyResult, type CompanyReadResult, type CompaniesPage, type CompanyContactsPage } from '@/lib/validations/companies';
async function get(path: string): Promise<unknown> {const response=await fetch(path,{cache:'no-store',credentials:'same-origin',signal:AbortSignal.timeout(15000)});return response.json();}
const unavailable={status:'unavailable' as const,message:'Lecture indisponible. Votre saisie est conservée. Réessayez.'};
export async function fetchCompany(id:string):Promise<CompanyReadResult>{try{return companyReadSchema.parse(await get(`/api/companies?id=${encodeURIComponent(id)}`));}catch{return unavailable;}}
export async function fetchCompanies(page:number):Promise<CompaniesPage>{try{return companiesPageSchema.parse(await get(`/api/companies?page=${page}`));}catch{return unavailable;}}
export async function fetchCompanyContacts(id:string,page:number):Promise<CompanyContactsPage>{try{return companyContactsPageSchema.parse(await get(`/api/companies?company_id=${encodeURIComponent(id)}&page=${page}`));}catch{return unavailable;}}
export async function sendCompanyCommand(command:CompanyCommand):Promise<CompanyResult>{try{const response=await fetch('/api/companies/command',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify(command),signal:AbortSignal.timeout(15000)});return companyResultSchema.parse(await response.json());}catch{return {status:'unavailable',message:'Confirmation non reçue. Réessayez la même commande ; votre saisie est conservée.'};}}
